#ifndef POSTSERVICES_CONFIG_H
#define POSTSERVICES_CONFIG_H

#include <string>

const std::string postEndpoint = "http://example.com/"; // Replace with actual endpoint
const std::string postPath = "sensors/"; // Replace with actual path
const std::string postIdKey = "/readings"; // Key for board ID in POST data


#endif // POSTSERVICES_CONFIG_H